<form method="post" action="{{ path('reservation_spa_delete', {'id': reservation_spa.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reservation_spa.id) }}">
    <button class="btn">Delete</button>
</form>
