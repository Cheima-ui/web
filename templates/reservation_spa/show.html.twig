{% extends 'base.html.twig' %}

{% block title %}ReservationSpa{% endblock %}

{% block body %}
    <h1>ReservationSpa</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reservation_spa.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ reservation_spa.Nom }}</td>
            </tr>
            <tr>
                <th>Mail</th>
                <td>{{ reservation_spa.mail }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ reservation_spa.date ? reservation_spa.date|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('reservation_spa_index') }}">back to list</a>

    <a href="{{ path('reservation_spa_edit', {'id': reservation_spa.id}) }}">edit</a>

    {{ include('reservation_spa/_delete_form.html.twig') }}
{% endblock %}
