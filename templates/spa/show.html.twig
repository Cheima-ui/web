

{% block title %}Spa{% endblock %}

{% block body %}
    <h1>Spa</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ spa.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ spa.type }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ spa.image }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ spa.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('spa_index') }}">back to list</a>

    <a href="{{ path('spa_edit', {'id': spa.id}) }}">edit</a>

    {{ include('spa/_delete_form.html.twig') }}
{% endblock %}
