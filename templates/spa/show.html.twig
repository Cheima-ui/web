{% extends 'base.html.twig' %}

{% block title %}SPA{% endblock %}

{% block body %}
    <h1>SPA</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ s_p_a.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ s_p_a.type }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ s_p_a.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('s_p_a_index') }}">back to list</a>

    <a href="{{ path('s_p_a_edit', {'id': s_p_a.id}) }}">edit</a>

    {{ include('s_p_a/_delete_form.html.twig') }}
{% endblock %}
