{% extends 'base.html.twig' %}

{% block title %}Edit SPA{% endblock %}

{% block body %}
    <h1>Edit SPA</h1>

    {{ include('s_p_a/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('s_p_a_index') }}">back to list</a>

    {{ include('s_p_a/_delete_form.html.twig') }}
{% endblock %}
